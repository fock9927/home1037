# home1037

## 自動單字收集示範網站

這個專案包含一個簡單的靜態示範頁面，可以貼上英文文章，系統會自動收錄單字並顯示出單字清單。

### 使用方式

1. 開啟 `index.html`。
2. 在左側記事區貼入文章。
3. 系統會自動抽取單字並在右側顯示。
4. 點擊單字可以標記為「已學過」。
5. 點擊單字旁的「翻譯」按鈕可查看中文翻譯。
6. 點選「匯出單字清單」可下載 `.txt` 檔案。

### 檔案內容

- `index.html`：主頁面。
- `styles.css`：樣式。
- `app.js`：自動擷取單字和互動邏輯。
